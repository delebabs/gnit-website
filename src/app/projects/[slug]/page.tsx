import { notFound } from 'next/navigation'; import {projects} from '@/data/projects'; import PageHero from '@/components/ui/PageHero';
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}));}
export default async function Project({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const p=projects.find(x=>x.slug===slug); if(!p)notFound(); return <><PageHero badge={p.category} title={p.title} description={p.description}/><section className='section'><div className='container grid gap-6 md:grid-cols-4'>{[['Capacity',p.capacity],['Technology',p.technology],['Duration',p.duration],['Location',p.location]].map(([k,v])=><div key={k} className='card p-7'><div className='text-sm text-slate-500'>{k}</div><div className='mt-2 text-xl font-bold'>{v}</div></div>)}</div></section></>}
