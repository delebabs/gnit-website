'use client';
import Link from 'next/link';
import { Menu, X } from 'lucide-react';
import { useState } from 'react';
const links=[['About','/about'],['Services','/services'],['Projects','/projects'],['Industries','/industries'],['Clients','/clients'],['Careers','/careers'],['News','/news'],['Contact','/contact']];
export default function Navbar(){ const [open,setOpen]=useState(false); return <header className="fixed inset-x-0 top-0 z-50 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="container flex h-20 items-center justify-between"><Link href="/" className="text-3xl font-black tracking-tight text-gnit-navy">GNIT</Link><nav className="hidden items-center gap-7 lg:flex">{links.map(([label,href])=><Link key={href} href={href} className="text-sm font-semibold text-slate-700 hover:text-gnit-gold">{label}</Link>)}<Link href="/request-quote" className="rounded-xl bg-gnit-gold px-5 py-3 text-sm font-bold text-white">Request Quote</Link></nav><button className="lg:hidden" onClick={()=>setOpen(!open)} aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></div>{open&&<div className="border-t bg-white px-6 py-5 lg:hidden">{links.map(([label,href])=><Link onClick={()=>setOpen(false)} key={href} href={href} className="block border-b py-4 font-semibold">{label}</Link>)}<Link onClick={()=>setOpen(false)} href="/request-quote" className="mt-4 block rounded-xl bg-gnit-gold px-5 py-3 text-center font-bold text-white">Request Quote</Link></div>}</header> }
