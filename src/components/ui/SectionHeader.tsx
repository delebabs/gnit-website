export default function SectionHeader({badge,title,description,center=false}:{badge:string;title:string;description?:string;center?:boolean}){return <div className={center?'mx-auto max-w-3xl text-center':'max-w-3xl'}><span className="font-semibold uppercase tracking-[0.3em] text-gnit-gold">{badge}</span><h2 className="mt-5 text-4xl font-black text-slate-900 md:text-5xl">{title}</h2>{description&&<p className="mt-5 text-lg leading-8 text-slate-600">{description}</p>}</div>}
