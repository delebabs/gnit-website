'use client'; import {useMemo,useState} from 'react'; import Link from 'next/link'; import {searchIndex} from '@/lib/search';
export default function Search(){const [q,setQ]=useState(''); const results=useMemo(()=>q.trim()?searchIndex.filter(x=>`${x.title} ${x.description} ${x.category}`.toLowerCase().includes(q.toLowerCase())):[],[q]);return <section className='section pt-32'><div className='container max-w-4xl'><h1 className='text-5xl font-black'>Search GNIT</h1><input value={q} onChange={e=>setQ(e.target.value)} placeholder='Search services, projects, articles and careers...' className='mt-8 w-full rounded-2xl border px-5 py-4'/><div className='mt-8 space-y-4'>{results.map(x=><Link href={x.href} key={x.href+x.title} className='block rounded-2xl border p-5 hover:bg-slate-50'><div className='text-xs font-bold uppercase text-gnit-gold'>{x.category}</div><div className='mt-2 text-xl font-bold'>{x.title}</div><p className='mt-2 text-slate-600'>{x.description}</p></Link>)}</div></div></section>}
